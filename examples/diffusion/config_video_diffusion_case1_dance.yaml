hydra:
  searchpath:
    - file://verl/trainer/config

defaults:
  - ppo_trainer
  - _self_

data:
  train_files: null
  val_files: null
  train_batch_size: 16
  gen_batch_size: 16
  val_batch_size: 1
  dataloader_num_workers: 0
  data_json_path: /share/models/dancegrpo/rl_embeddings/videos2caption.json
  t: 28
  cfg: 0.0

algorithm:
  adv_estimator: grpo
  use_kl_in_reward: false

critic:
  enable: false

reward_model:
  enable: false

actor_rollout_ref:
  model:
    path: /share/models/dancegrpo/HunyuanVideo
    trust_remote_code: false
    use_remove_padding: true
    enable_gradient_checkpointing: true
  actor:
    strategy: fsdp
    ppo_mini_batch_size: 16
    ppo_micro_batch_size_per_gpu: 1
    use_kl_loss: false
    dance_case1_mode: true
    gradient_accumulation_steps: 2
    extra:
      diffusion: true
      gradient_checkpointing: true
      dance:
        pretrained_model_name_or_path: /share/models/dancegrpo/HunyuanVideo
        vae_model_path: /share/models/dancegrpo/HunyuanVideo
        model_type: hunyuan_hf
        master_weight_type: bf16
        use_videoalign: true
        videoalign_ckpt_path: /share/models/dancegrpo/videoalign_ckpt
        timestep_fraction: 0.5
        fsdp_sharding_strategy: full
        actor_prompt_batch_count: 2
    fsdp_config:
      param_offload: false
      optimizer_offload: false
    optim:
      lr: 1.0e-6
      weight_decay: 1.0e-2
      warmup_style: constant
      lr_warmup_steps: 0
  rollout:
    name: diffusion
    do_sample: true
    n: 1
    log_prob_micro_batch_size_per_gpu: 1
    guidance_scale: 4.5
    num_steps: 28
    num_frames: 33
    height: 256
    width: 256
    sampling_steps: 28
    shift: 5.0
    eta: 1.0
    fps: 24
    num_generations: 4
    use_group: true
    use_same_noise: false
    bestofn: 4
    vq_coef: 0.5
    mq_coef: 0.5
  ref:
    diffusion: true
    fsdp_config:
      param_offload: false
    log_prob_micro_batch_size_per_gpu: 1

trainer:
  total_epochs: 1
  total_training_steps: null
  max_train_steps: 4
  project_name: dance_case1
  experiment_name: case1_dance
  logger: ["console"]
  nnodes: 1
  n_gpus_per_node: 8
  test_freq: -1
  val_freq: -1
  val_before_train: false
  save_freq: -1
  diffusion: true
  disaggregate: true
  disaggregate_actor_n_gpus_per_node: 4
  disaggregate_rollout_ref_n_gpus_per_node: 4
  dist_master_addr: null
  dist_master_port: null
  pipelined_micro_batch: true
